input:
  directory: data/lb_test_data
  n_ranks: 4

viz:
  # x_ranks: 4
  # y_ranks: 4
  object_jitter: 0.5
  rank_qoi: load
  object_qoi: load
  save_meshes: true
  save_pngs: true
  force_continuous_object_qoi: true

output:
  directory: output
  file_stem: lb_test
  window_size: 2000
