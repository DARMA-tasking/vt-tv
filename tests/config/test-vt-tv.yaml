# Specify input
input:
  directory: tests/data/synthetic_lb_data
  n_ranks: 4


# Specify output
output_dir: ../output
output_file_stem: output_file

viz:
  x_ranks: 2
  y_ranks: 2
  z_ranks: 1
  object_jitter: 0.5
  rank_qoi: load
  object_qoi: load
  save_meshes: true
  save_pngs: true
  force_continuous_object_qoi: true

output:
  directory: output
  file_stem: tv